/* This site has no JavaScript behaviour — it's pure static HTML + CSS.
   This file exists only as a Rollup entry point so the build can process
   and extract the stylesheet. Don't add logic here unless you actually
   want client-side JS on the page. */
import './styles/main.css';
